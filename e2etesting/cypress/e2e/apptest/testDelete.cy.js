describe('Delete ', () => {
    beforeEach(() => {
      cy.visit('http://localhost:3000/updatequestion', { timeout: 30000 })
    });
      
        it('Adding fields to QUESTION...', () => {
                cy.Delete()
        })


})